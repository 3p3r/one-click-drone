version: 1
update_configs:
  - package_manager: 'javascript'
    directory: '/'
    update_schedule: 'live'
    target_branch: 'develop'
    commit_message:
      # don't use cache when building dependabot PRs
      prefix: '[NO CACHE] chore'
      include_scope: true
